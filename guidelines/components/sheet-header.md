# sheet-header

> To be populated via Figma MCP inspection.

## Template sections
- When to use / Decision tree
- Variants & Node IDs
- Props (exact Figma names + accepted values)
- Instance overrides (internal layer names)
- Composition rules
